# Default configuration for HSEM integration tests
default_config:
  host: "192.168.1.100"
  username: "test_user"
  password: "test_password"
  scan_interval: 30

battery_settings:
  rated_capacity: 10.0
  max_charging_power: 5000
  grid_charge_cutoff_soc: 85
  conversion_loss: 5
  minimum_soc: 5
  maximum_soc: 100

working_mode_settings:
  start_hour: 12
  stop_hour: 17
  min_charging_duration: 1
  weather_factor: 0.8
  price_threshold: 0.25

logging:
  default: info
  logs:
    custom_components.hsem: debug
