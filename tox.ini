[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
    homeassistant
commands = pytest --cov=./custom_components/hsem --cov-report=xml --cov-config=tox.ini --cov-branch
